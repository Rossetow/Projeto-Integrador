import { FlatList, Text, View } from "react-native"

const Home = () => {
    return(
        <View>
            <Text>Hello</Text>
        {/* <FlatList
      data={}
      renderItem={({ item }) => (
        <Post
        posts={item}
        />
      )}
      keyExtractor={(item) => item.idPost}
      /> */}
        </View>
    )
}

export default Home